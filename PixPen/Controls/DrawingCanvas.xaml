<UserControl x:Class="PixPen.Controls.DrawingCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Focusable="True">
    <!--
        外側 Grid: ビューポート（ダークグレー背景・クリップ）
        Canvas を挟むことで ContentGrid の measure 制約を Infinity にする。
        Infinity 制約下では Stretch な子要素（GridOverlay 等）が 0×0 を要求するため
        ContentGrid のサイズがウィンドウサイズに引きずられない。
    -->
    <Grid Background="#FF404040" ClipToBounds="True" x:Name="ViewportGrid">
        <Canvas x:Name="ViewportCanvas">
            <!-- キャンバスコンテンツ（ズーム・パン対象） -->
            <Grid x:Name="ContentGrid">
                <Grid.RenderTransform>
                    <MatrixTransform x:Name="ViewTransform"/>
                </Grid.RenderTransform>

                <!-- 透過チェッカーボード背景 -->
                <!-- x:Name で CheckerBrush を参照し、ズーム時にタイルサイズを補正する -->
                <Rectangle x:Name="BackgroundRect">
                    <Rectangle.Fill>
                        <DrawingBrush x:Name="CheckerBrush"
                                      TileMode="Tile"
                                      Viewport="0,0,16,16"
                                      ViewportUnits="Absolute">
                            <DrawingBrush.Drawing>
                                <DrawingGroup>
                                    <GeometryDrawing Brush="#FFB0B0B0">
                                        <GeometryDrawing.Geometry>
                                            <RectangleGeometry Rect="0,0,16,16"/>
                                        </GeometryDrawing.Geometry>
                                    </GeometryDrawing>
                                    <GeometryDrawing Brush="#FF808080">
                                        <GeometryDrawing.Geometry>
                                            <GeometryGroup>
                                                <RectangleGeometry Rect="0,0,8,8"/>
                                                <RectangleGeometry Rect="8,8,8,8"/>
                                            </GeometryGroup>
                                        </GeometryDrawing.Geometry>
                                    </GeometryDrawing>
                                </DrawingGroup>
                            </DrawingBrush.Drawing>
                        </DrawingBrush>
                    </Rectangle.Fill>
                </Rectangle>

                <!-- 合成表示用イメージ -->
                <Image x:Name="CompositeImage"
                       RenderOptions.BitmapScalingMode="NearestNeighbor"
                       Stretch="None"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Top"/>

                <!-- グリッドオーバーレイ -->
                <Canvas x:Name="GridOverlay" IsHitTestVisible="False"/>

                <!-- 選択範囲オーバーレイ -->
                <Canvas x:Name="SelectionOverlay" IsHitTestVisible="False">
                    <Rectangle x:Name="SelectionRect"
                               Stroke="#FF0080FF"
                               StrokeThickness="1"
                               StrokeDashArray="4,4"
                               Fill="Transparent"
                               Visibility="Collapsed"/>
                </Canvas>
            </Grid>
        </Canvas>
    </Grid>
</UserControl>
