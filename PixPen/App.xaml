<Application x:Class="PixPen.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PixPen"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- index 0: テーマカラー辞書（ThemeService が実行時に差し替える） -->
                <ResourceDictionary Source="Themes/LightTheme.xaml"/>
                <!-- index 1: コントロールスタイル（常にロード） -->
                <ResourceDictionary Source="Themes/ControlStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- ─── コンバーター ─────────────────────────────────────── -->
            <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
            <local:NullToBoolConverter    x:Key="NullToBoolConverter"/>
            <local:StringToColorConverter x:Key="StringToColorConverter"/>
            <local:ColorMatchConverter    x:Key="ColorMatchConverter"/>

            <!-- ─── ToolBar の RadioButton スタイル ───────────────────── -->
            <!-- ToolRadioStyle: ToolBar 専用。テーマカラーを DynamicResource で参照 -->
            <Style x:Key="ToolRadioStyle" TargetType="RadioButton">
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="border"
                                    Background="Transparent"
                                    BorderThickness="1"
                                    BorderBrush="Transparent"
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="3">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="border" Property="Background"   Value="#FF0078D7"/>
                                    <Setter TargetName="border" Property="BorderBrush"  Value="#FF005A9E"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background"   Value="{DynamicResource App.Hover}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
