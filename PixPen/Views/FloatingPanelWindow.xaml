<Window x:Class="PixPen.Views.FloatingPanelWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        ShowInTaskbar="False"
        ResizeMode="CanResize"
        MinWidth="180" MinHeight="120">
    <DockPanel>
        <!-- カスタムタイトルバー -->
        <Border DockPanel.Dock="Top"
                Background="{DynamicResource App.FloatBar.Bg}"
                Padding="8,0,4,0">
            <DockPanel Height="28">
                <Button x:Name="DockButton"
                        DockPanel.Dock="Right"
                        Click="OnDock"
                        ToolTip="サイドパネルにドッキングする"
                        Padding="6,2"
                        VerticalAlignment="Center"
                        Background="{DynamicResource App.FloatBtn.Bg}"
                        BorderBrush="{DynamicResource App.FloatBtn.Border}"
                        Foreground="{DynamicResource App.FloatBtn.Fg}"
                        FontSize="11"
                        Content="ドッキング"
                        Style="{x:Null}"/>
                <TextBlock x:Name="TitleText"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource App.FloatBar.Fg}"
                           FontSize="12"
                           FontWeight="SemiBold"/>
            </DockPanel>
        </Border>
        <!-- パネルコンテンツ -->
        <ContentControl x:Name="PanelHost"/>
    </DockPanel>
</Window>
