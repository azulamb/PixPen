<Window x:Class="PixPen.Views.Dialogs.DocumentSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ドキュメントの設定"
        Width="320" Height="240"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Text="タイトル" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="4"
                 Text="{Binding Document.Title, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="1" Text="幅 (px)" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="4" x:Name="WidthBox"
                 Text="{Binding Document.Width}"/>

        <TextBlock Grid.Row="2" Text="高さ (px)" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="4" x:Name="HeightBox"
                 Text="{Binding Document.Height}"/>

        <TextBlock Grid.Row="3" Text="DPI" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" Margin="4"
                 Text="{Binding Document.Dpi}"/>

        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="72" Margin="4" Click="OnOk"/>
            <Button Content="キャンセル" Width="72" Margin="4" Click="OnCancel"/>
        </StackPanel>
    </Grid>
</Window>
