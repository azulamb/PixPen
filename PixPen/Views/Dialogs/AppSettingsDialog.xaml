<Window x:Class="PixPen.Views.Dialogs.AppSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:m="clr-namespace:PixPen.Models"
        Title="設定"
        Width="380" Height="300"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Text="Undo メモリ上限 (MB)" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="4"
                 Text="{Binding UndoMemoryLimitMb, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="1" Text="最大レイヤー数" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="4"
                 Text="{Binding MaxLayers}"/>

        <TextBlock Grid.Row="2" Text="デフォルト幅 (px)" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="4"
                 Text="{Binding DefaultWidth}"/>

        <TextBlock Grid.Row="3" Text="デフォルト高さ (px)" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" Margin="4"
                 Text="{Binding DefaultHeight}"/>

        <TextBlock Grid.Row="4" Text="サイドパネル位置" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="4" Grid.Column="1" Margin="4"
                  SelectedItem="{Binding SidePanelSide}">
            <m:SidePanelSide>Left</m:SidePanelSide>
            <m:SidePanelSide>Right</m:SidePanelSide>
        </ComboBox>

        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="72" Margin="4" Click="OnOk"/>
            <Button Content="キャンセル" Width="72" Margin="4" Click="OnCancel"/>
        </StackPanel>
    </Grid>
</Window>
