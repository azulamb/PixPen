<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ════ Window ════ -->
    <Style TargetType="Window">
        <Setter Property="Background" Value="{DynamicResource App.Bg}"/>
        <Setter Property="Foreground" Value="{DynamicResource App.Fg}"/>
    </Style>

    <!-- ════ TextBlock ════ -->
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource App.Fg}"/>
    </Style>

    <!-- ════ Label ════ -->
    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{DynamicResource App.Fg}"/>
    </Style>

    <!-- ════ Button ════ -->
    <Style TargetType="Button">
        <Setter Property="Background"              Value="{DynamicResource App.Ctrl.Bg}"/>
        <Setter Property="Foreground"              Value="{DynamicResource App.Fg}"/>
        <Setter Property="BorderBrush"             Value="{DynamicResource App.Border}"/>
        <Setter Property="BorderThickness"         Value="1"/>
        <Setter Property="Padding"                 Value="6,2"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment"   Value="Center"/>
        <Setter Property="FocusVisualStyle"        Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource App.Hover}"/>
                            <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource App.Accent}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource App.Pressed}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ════ ToggleButton ════ -->
    <Style TargetType="ToggleButton">
        <Setter Property="Background"      Value="{DynamicResource App.Ctrl.Bg}"/>
        <Setter Property="Foreground"      Value="{DynamicResource App.Fg}"/>
        <Setter Property="BorderBrush"     Value="{DynamicResource App.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding"         Value="6,2"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource App.Hover}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource App.Accent}"/>
                            <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource App.Accent}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ════ TextBox ════ -->
    <Style TargetType="TextBox">
        <Setter Property="Background"      Value="{DynamicResource App.Ctrl.Bg}"/>
        <Setter Property="Foreground"      Value="{DynamicResource App.Fg}"/>
        <Setter Property="BorderBrush"     Value="{DynamicResource App.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding"         Value="2,1"/>
        <Setter Property="CaretBrush"      Value="{DynamicResource App.Fg}"/>
        <Setter Property="SelectionBrush"  Value="{DynamicResource App.Selected.Bg}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="{TemplateBinding Padding}"
                                      Focusable="False"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource App.Accent}"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource App.Accent}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource App.Disabled.Bg}"/>
                            <Setter Property="Foreground" Value="{DynamicResource App.Disabled.Fg}"/>
                        </Trigger>
                        <Trigger Property="IsReadOnly" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource App.Disabled.Bg}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ════ CheckBox ════ -->
    <Style TargetType="CheckBox">
        <Setter Property="Foreground"      Value="{DynamicResource App.Fg}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border x:Name="box" Width="14" Height="14"
                                Background="{DynamicResource App.Ctrl.Bg}"
                                BorderBrush="{DynamicResource App.Border}"
                                BorderThickness="1" SnapsToDevicePixels="True">
                            <Path x:Name="check"
                                  Data="M2,7 L5,10 L12,3"
                                  Stroke="{DynamicResource App.Accent}"
                                  StrokeThickness="2"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                  Visibility="Collapsed"/>
                        </Border>
                        <ContentPresenter Margin="4,0,0,0"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource App.Accent}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ════ RadioButton（ダイアログ用デフォルト）════ -->
    <Style TargetType="RadioButton">
        <Setter Property="Foreground"      Value="{DynamicResource App.Fg}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border x:Name="circle" Width="14" Height="14"
                                CornerRadius="7"
                                Background="{DynamicResource App.Ctrl.Bg}"
                                BorderBrush="{DynamicResource App.Border}"
                                BorderThickness="1" SnapsToDevicePixels="True">
                            <Ellipse x:Name="dot" Width="6" Height="6"
                                     Fill="{DynamicResource App.Accent}"
                                     Visibility="Collapsed"/>
                        </Border>
                        <ContentPresenter Margin="4,0,0,0"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="dot" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="circle" Property="BorderBrush" Value="{DynamicResource App.Accent}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="circle" Property="BorderBrush" Value="{DynamicResource App.Accent}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ════ ComboBox ════ -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Background"      Value="Transparent"/>
        <Setter Property="Foreground"      Value="{DynamicResource App.Fg}"/>
        <Setter Property="Padding"         Value="4,2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="bd" Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource App.Selected.Bg}"/>
                            <Setter Property="Foreground" Value="{DynamicResource App.Selected.Fg}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Background"      Value="{DynamicResource App.Ctrl.Bg}"/>
        <Setter Property="Foreground"      Value="{DynamicResource App.Fg}"/>
        <Setter Property="BorderBrush"     Value="{DynamicResource App.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding"         Value="4,2"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                        </Grid.ColumnDefinitions>
                        <Border x:Name="bd" Grid.ColumnSpan="2"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True"/>
                        <!-- 通常表示 -->
                        <ContentPresenter x:Name="ContentSite"
                                          Grid.Column="0"
                                          Margin="{TemplateBinding Padding}"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          VerticalAlignment="Center"
                                          IsHitTestVisible="False"/>
                        <!-- 編集可能テキスト -->
                        <TextBox x:Name="PART_EditableTextBox"
                                 Grid.Column="0"
                                 Margin="{TemplateBinding Padding}"
                                 Background="Transparent"
                                 Foreground="{TemplateBinding Foreground}"
                                 BorderThickness="0"
                                 IsReadOnly="{TemplateBinding IsReadOnly}"
                                 VerticalAlignment="Center"
                                 Visibility="Collapsed"/>
                        <!-- ドロップダウン矢印 -->
                        <Path Grid.Column="1"
                              Data="M0,0 L5,0 L2.5,3 Z"
                              Fill="{DynamicResource App.Fg}"
                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <!-- トグルボタン（クリック領域） -->
                        <ToggleButton Grid.ColumnSpan="2"
                                      Background="Transparent" BorderThickness="0"
                                      Style="{x:Null}"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay,
                                          RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                      ClickMode="Press"/>
                        <!-- ドロップダウンポップアップ -->
                        <Popup x:Name="PART_Popup"
                               Grid.ColumnSpan="2"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               Placement="Bottom"
                               AllowsTransparency="True"
                               PopupAnimation="Slide">
                            <Border Background="{DynamicResource App.Popup.Bg}"
                                    BorderBrush="{DynamicResource App.Popup.Border}"
                                    BorderThickness="1"
                                    MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <ScrollViewer>
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="ContentSite"          Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource App.Accent}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ════ ListBox / ListBoxItem ════ -->
    <Style TargetType="ListBoxItem">
        <Setter Property="Foreground"      Value="{DynamicResource App.Fg}"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="bd" Background="Transparent" Padding="2,1"
                            SnapsToDevicePixels="True">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource App.Hover}"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="Selector.IsSelectionActive" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource App.Selected.Bg}"/>
                            <Setter Property="Foreground" Value="{DynamicResource App.Selected.Fg}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="Selector.IsSelectionActive" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource App.Selected.Inactive.Bg}"/>
                            <Setter Property="Foreground" Value="{DynamicResource App.Selected.Inactive.Fg}"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ListBox">
        <Setter Property="Background"      Value="{DynamicResource App.Ctrl.Bg}"/>
        <Setter Property="Foreground"      Value="{DynamicResource App.Fg}"/>
        <Setter Property="BorderBrush"     Value="{DynamicResource App.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <ScrollViewer Focusable="False" Padding="{TemplateBinding Padding}">
                            <ItemsPresenter/>
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ════ ListView / ListViewItem ════ -->
    <Style TargetType="ListViewItem" BasedOn="{StaticResource {x:Type ListBoxItem}}"/>
    <Style TargetType="ListView"     BasedOn="{StaticResource {x:Type ListBox}}"/>

    <!-- ════ GroupBox ════ -->
    <Style TargetType="GroupBox">
        <Setter Property="Foreground"      Value="{DynamicResource App.Fg}"/>
        <Setter Property="BorderBrush"     Value="{DynamicResource App.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GroupBox">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <!-- ヘッダー -->
                        <Border Grid.Row="0"
                                Background="{DynamicResource App.Panel.Bg}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1,1,1,0"
                                Padding="6,3">
                            <ContentPresenter Content="{TemplateBinding Header}"
                                              ContentTemplate="{TemplateBinding HeaderTemplate}"
                                              RecognizesAccessKey="True"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <!-- コンテンツ -->
                        <Border Grid.Row="1"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1,0,1,1"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ════ TabControl ════ -->
    <Style TargetType="TabControl">
        <Setter Property="Background"      Value="{DynamicResource App.Tab.Active.Bg}"/>
        <Setter Property="BorderBrush"     Value="{DynamicResource App.Tab.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding"         Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <DockPanel>
                        <!-- タブ行 -->
                        <Border DockPanel.Dock="Top"
                                Background="{DynamicResource App.Tab.Inactive.Bg}"
                                BorderBrush="{DynamicResource App.Tab.Border}"
                                BorderThickness="0,0,0,1">
                            <TabPanel IsItemsHost="True"/>
                        </Border>
                        <!-- コンテンツ -->
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="0">
                            <ContentPresenter ContentSource="SelectedContent"/>
                        </Border>
                    </DockPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ════ TabItem ════ -->
    <Style TargetType="TabItem">
        <Setter Property="Foreground"      Value="{DynamicResource App.Tab.Inactive.Fg}"/>
        <Setter Property="Background"      Value="{DynamicResource App.Tab.Inactive.Bg}"/>
        <Setter Property="Padding"         Value="8,4"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{DynamicResource App.Tab.Border}"
                            BorderThickness="1,1,1,0"
                            Padding="{TemplateBinding Padding}"
                            Margin="0,0,2,0">
                        <ContentPresenter ContentSource="Header"
                                          RecognizesAccessKey="True"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource App.Tab.Active.Bg}"/>
                            <Setter Property="Foreground" Value="{DynamicResource App.Tab.Active.Fg}"/>
                            <Setter TargetName="bd" Property="BorderThickness" Value="1,2,1,0"/>
                            <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource App.Accent}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource App.Hover}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ════ Menu ════ -->
    <Style TargetType="Menu">
        <Setter Property="Background" Value="{DynamicResource App.Menu.Bg}"/>
        <Setter Property="Foreground" Value="{DynamicResource App.Fg}"/>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{DynamicResource App.Fg}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="bd" Background="{TemplateBinding Background}"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="MenuItemIconColumn"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="MenuItemIGTColumn"/>
                                <ColumnDefinition Width="14"/>
                            </Grid.ColumnDefinitions>
                            <!-- アイコン列（左マージン） -->
                            <ContentPresenter Grid.Column="0" Width="22"
                                              Content="{TemplateBinding Icon}"
                                              Margin="3,0" VerticalAlignment="Center"/>
                            <!-- ヘッダーテキスト -->
                            <ContentPresenter Grid.Column="1"
                                              Content="{TemplateBinding Header}"
                                              ContentStringFormat="{TemplateBinding HeaderStringFormat}"
                                              Margin="6,3"
                                              RecognizesAccessKey="True"
                                              VerticalAlignment="Center"/>
                            <!-- ショートカットキー表示 -->
                            <TextBlock Grid.Column="2"
                                       Text="{TemplateBinding InputGestureText}"
                                       Margin="6,3,6,3"
                                       Foreground="{DynamicResource App.Subtle.Fg}"
                                       VerticalAlignment="Center"/>
                            <!-- サブメニュー矢印 -->
                            <Path Grid.Column="3"
                                  x:Name="submenuArrow"
                                  Data="M0,0 L4,4 L0,8 Z"
                                  Fill="{DynamicResource App.Fg}"
                                  HorizontalAlignment="Center" VerticalAlignment="Center"
                                  Visibility="Collapsed"/>
                            <!-- サブメニューポップアップ -->
                            <Popup x:Name="PART_Popup" Grid.Column="0"
                                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   Placement="Right" AllowsTransparency="True" PopupAnimation="Fade">
                                <Border Background="{DynamicResource App.Popup.Bg}"
                                        BorderBrush="{DynamicResource App.Popup.Border}"
                                        BorderThickness="1">
                                    <StackPanel IsItemsHost="True" Grid.IsSharedSizeScope="True"/>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource App.Menu.Hover}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource App.Disabled.Fg}"/>
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="submenuArrow" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource App.Selected.Inactive.Bg}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ════ Separator（メニュー内） ════ -->
    <Style TargetType="Separator">
        <Setter Property="Background" Value="{DynamicResource App.Menu.Separator}"/>
        <Setter Property="Height"     Value="1"/>
        <Setter Property="Margin"     Value="4,2"/>
    </Style>

    <!-- ════ ToolBarTray / ToolBar ════ -->
    <Style TargetType="ToolBarTray">
        <Setter Property="Background" Value="{DynamicResource App.Toolbar.Bg}"/>
    </Style>

    <Style TargetType="ToolBar">
        <Setter Property="Background" Value="{DynamicResource App.Toolbar.Bg}"/>
        <Setter Property="Foreground" Value="{DynamicResource App.Fg}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolBar">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{DynamicResource App.Border}"
                            BorderThickness="0,0,0,1"
                            Padding="2">
                        <ToolBarPanel IsItemsHost="True"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ════ StatusBar ════ -->
    <Style TargetType="StatusBar">
        <Setter Property="Background" Value="#FF007ACC"/>
        <Setter Property="Foreground" Value="#FFFFFFFF"/>
    </Style>

    <Style TargetType="StatusBarItem">
        <Setter Property="Foreground" Value="#FFFFFFFF"/>
    </Style>

    <!-- ════ GridSplitter ════ -->
    <Style TargetType="GridSplitter">
        <Setter Property="Background" Value="{DynamicResource App.Splitter}"/>
    </Style>

    <!-- ════ Slider ════ -->
    <Style TargetType="Slider">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <!-- トラック -->
                        <Border Grid.Row="1" Height="4"
                                Background="{DynamicResource App.Border}"
                                CornerRadius="2"/>
                        <!-- PART_Track -->
                        <Track x:Name="PART_Track" Grid.Row="1">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Style="{x:Null}" Opacity="0" Focusable="False"/>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Style="{x:Null}" Opacity="0" Focusable="False"/>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Width="12" Height="12" Focusable="False">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Ellipse x:Name="e"
                                                     Width="12" Height="12"
                                                     Fill="{DynamicResource App.Accent}"
                                                     Stroke="{DynamicResource App.Border}"
                                                     StrokeThickness="1"/>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="e" Property="Fill" Value="{DynamicResource App.Hover}"/>
                                                </Trigger>
                                                <Trigger Property="IsDragging" Value="True">
                                                    <Setter TargetName="e" Property="Fill" Value="{DynamicResource App.Pressed}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ════ ScrollBar ════ -->
    <Style x:Key="ScrollBarThumbStyle" TargetType="Thumb">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="bd"
                            Background="{DynamicResource App.Scrollbar.Thumb}"
                            CornerRadius="3" Margin="2"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource App.Scrollbar.Thumb.Hover}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="{DynamicResource App.Scrollbar.Track}"/>
        <Setter Property="Width"      Value="10"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="{TemplateBinding Background}">
                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Style="{x:Null}" Opacity="0" Command="ScrollBar.PageUpCommand"/>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Style="{x:Null}" Opacity="0" Command="ScrollBar.PageDownCommand"/>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource ScrollBarThumbStyle}"/>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter Property="Width"  Value="Auto"/>
                            <Setter Property="Height" Value="10"/>
                            <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
